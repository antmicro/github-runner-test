on: [push]

name: test

jobs:
  docker:
    container: debian:11
    runs-on: [self-hosted, Linux, X64]
    env:
      GHA_PREEMPTIBLE: "false"
    steps:
    - run: echo "RUNNING..."

  full-docker:
    container: docker.io/library/debian:11
    runs-on: [self-hosted, Linux, X64]
    env:
      GHA_PREEMPTIBLE: "false"
    steps:
    - run: echo "RUNNING..."
  
  gcr:
    container: gcr.io/google-containers/debian-base:v1.0.1
    runs-on: [self-hosted, Linux, X64]
    env:
      GHA_PREEMPTIBLE: "false"
    steps:
    - run: echo "RUNNING..."

  ghcr:
    container: ghcr.io/goover/debian:11-bullseye
    runs-on: [self-hosted, Linux, X64]
    env:
      GHA_PREEMPTIBLE: "false"
    steps:
    - run: echo "RUNNING..."
