on: [push]

name: test

jobs:
  centos:
    container: centos:8
    runs-on: [self-hosted, Linux, X64, gcp-disk-test]
    env:
      GHA_EXTERNAL_DISK: "auxdisk"
      GHA_PREEMPTIBLE: "false"
      KEYS: "${{ secrets.KEYS }}"
      
    steps:
    - run: df -h .
    - run: df -h /mnt/aux
    - run: ls -alh /mnt/aux
    - run: ls -lah .
