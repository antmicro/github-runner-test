on: [push]

name: test

jobs:

  secret-script:
    container: debian:bullseye
    runs-on: [self-hosted, Linux, X64]
    steps:
      - run: cat /etc/os-release
      - run: _secret_simple
      - run: printenv
