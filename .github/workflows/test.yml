on: [push]

name: test

jobs:

  secret-script:
    container: centos:8
    runs-on: [self-hosted, Linux, X64]
    steps:
    - name: checkout
      env:
        INPUT_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: _secret_custom_checkout
